One-line: The core rounded surface — white, soft-shadowed, generously rounded. Everything sits in one.

```jsx
<Card elevation="sm" padding="md">…</Card>
<Card interactive onClick={open}>Tap to open thread</Card>
```
Elevations `flat|sm|md|lg`; padding `none|sm|md|lg`. `interactive` adds a gentle hover lift.
