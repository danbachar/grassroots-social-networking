One-line: Rounded progress/capacity bar with optional label and percentage.

```jsx
<ProgressBar value={64} label="Message relayed" showValue />
```
Tones: primary, accent, success, warning. Sizes `sm|md|lg`.
