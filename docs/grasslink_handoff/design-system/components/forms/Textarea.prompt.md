One-line: Multi-line message/notes field; matches Input styling and focus ring.

```jsx
<Textarea label="Message" rows={5} placeholder="Write something to relay…" />
```
Vertically resizable. Supports `hint` and `error`.
